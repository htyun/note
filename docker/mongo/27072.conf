dbpath=/data/db
directoryperdb=true # 为每一个数据库按照数据库名建立文件夹存放
logappend=true # 以追加的方式记录日志
replSet=mongoreplset
bind_ip=10.27.0.72
port=27017
oplogSize=10000 # mongodb操作日志文件的最大大小。单位为Mb，默认为硬盘剩余空间的5%
fork=false # 以后台方式运行进程
noprealloc=true # 不预先分配存储